# SWARM ONLY — DEFERRED

This prompt is only used in swarm mode (--swarm). Swarm mode is deferred to
V1.0+. The Bailiff runs objective tests and enforces framework rules across
multiple competing agent worktrees. Not used in default (non-swarm) mode.
